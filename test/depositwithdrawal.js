const DepositWithdrawal = artifacts.require("DepositWithdrawal");

contract('DepositWithdrawal', (accounts) => {
  it('Deposit one NFT', async () => {
    const depositWithdrawal = await DepositWithdrawal.deployed();

  });


  it('Withdrawal', async () => {
    const depositWithdrawal = await depositWithdrawal.deployed();

  });



});
